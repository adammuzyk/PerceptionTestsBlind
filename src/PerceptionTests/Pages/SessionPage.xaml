<local:BasePage x:Class="PerceptionTests.Pages.SessionPage"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:local="clr-namespace:PerceptionTests.Pages"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                mc:Ignorable="d"
                d:DesignHeight="300"
                d:DesignWidth="300">
    <TabControl Name="MainTabControl">
        <TabControl.ItemContainerStyle>
            <Style TargetType="TabItem">
                <Setter Property="Visibility" Value="Collapsed"></Setter>
            </Style>
        </TabControl.ItemContainerStyle>

        <TabItem Name="FirstTabItem">
            <Grid>
                <TextBlock Text="{Binding IntroText}" TextWrapping="Wrap" />
                <Button Name="PlayButton" Click="PlayButton_OnClick" Height="50" VerticalAlignment="Bottom">Start</Button>
            </Grid>
        </TabItem>

        <TabItem Name="SecondTabItem">
            <Button Name="StopButton" Click="StopButton_OnClick" Content="{Binding PlaybackPrompt}" />
        </TabItem>

        <TabItem Name="ThirdTabItem">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding StatusMessage}" TextWrapping="Wrap" />
                <Button Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" Name="Play2Button" Click="PlayButton_OnClick" Height="50" Content="{Binding ActionButtonText}" />
            </Grid>
        </TabItem>
    </TabControl>
</local:BasePage>
